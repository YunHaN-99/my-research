# Pilot Bug Repair Notes Template v0

## 基本信息
- `session_id`：`PILOT_20260420_000`
- `participant_id`：`SELF`
- `task_id`：`T2_A1_bug_01`
- `condition`：`process_guided_workflow`

## 1. Symptom 重述
- 当前看到的问题是：
  DP 在处理第一列或最后一列时还会去读三方向父节点，导致 `j+1` 越界，或者在 `j=0` 时把负索引当成合法父节点。
- 理想行为应该是：
  每个位置只从合法父节点里选最小累计代价，边界列不能读越界，也不能偷偷把最后一列当成第一列的左父节点。

## 2. 关键不变量
- 我认为最关键的不变量是：
  `backtrack[i, j]` 必须始终落在合法列范围内，而且 DP 转移只能来自上一行与当前列相差不超过 `1` 的合法父节点。
- 当前哪里破坏了这个不变量：
  代码把 `j-1`、`j`、`j+1` 写死成三项列表，没有先按边界截断。

## 3. 最小修补计划
- 我准备修改的代码位置：
  `build_cumulative_cost_buggy()` 里构造 `prev` 和回写 `backtrack` 的那几行。
- 我为什么认为这是根因：
  symptom 已经直接指向边界列报错和路径跳变；这两种现象都能由“非法父节点被纳入比较”解释。
- 我不会去改的部分：
  不改函数签名，不改 `dp/backtrack` 的 shape 和 dtype，也不重写整段 DP。

## 4. 自检与回归
- 提交前我会检查：
  1. `j=0` 和 `j=w-1` 时不会越界。
  2. `backtrack` 的最小值不小于 `0`，最大值小于 `w`。
  3. 修补后仍保持“上一行合法父节点中取最小值”的原语义。
- 最小回归说明：
  用一个 `3x4` 的小能量矩阵跑一次，确认函数不再抛 `IndexError`，且 `backtrack` 全部落在 `[0, w-1]` 内。

## 5. 结果备注
- 当前 patch 是否已能运行：
  是。
- 最终提交代码路径 / 文件名：
  `pilot/dry_run_records/PILOT_20260420_000/src/patched_code.py`
- 仍不确定的地方：
  这次是单人 dry run，所以主观评分和研究者观察没有真实分离。
