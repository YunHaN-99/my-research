# diagnosis

问题原因：
- 高度缩放复用宽度逻辑时，转置轴顺序写错。
- 彩色图像应保持通道轴不变，只交换 H/W 轴。

定位方法：
1. 打印转置前后 shape，检查是否仍为 (H, W, C) 语义。
2. 验证两次转置后能否恢复原通道位置。

修复要点：
- RGB 图像使用 np.transpose(im, (1, 0, 2))。
- 灰度图像使用 np.transpose(im, (1, 0))。
