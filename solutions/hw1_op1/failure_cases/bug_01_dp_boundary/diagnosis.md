# diagnosis

问题原因：
- DP 转移时没有对 j-1 和 j+1 做边界截断。
- j=0 和 j=w-1 时会读取非法父节点，导致越界或错误父节点。

定位方法：
1. 打印每行 j=0 和 j=w-1 的候选父节点索引。
2. 对比正确实现应使用 left=max(j-1,0), right=min(j+1,w-1)。

修复要点：
- 只在合法父节点集合中取最小值。
- backtrack 记录必须回写到合法列范围。
