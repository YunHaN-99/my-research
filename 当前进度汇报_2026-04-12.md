# 当前进度汇报（对齐 Timeline）

日期：`2026-04-12`

## 一、当前整体判断
截至目前，项目已经完成首轮主案例 A1 / A2 的闭环实验链路，已经不再是“想法整理”阶段，而是进入了“主案例证据链已形成，中期材料需要收口”的阶段。

当前一句话阶段判断应统一写成：

> A1 / A2 首轮主案例闭环已完成；当前中期重点是收束模板库、检查清单、错误分类、题库页和原型入口。

如果严格按照 `Timeline.docx` 的阶段表述来判断：
- 当前已经完成 `P0` 和 `P1` 的核心任务。
- `P2` 中与中期材料直接相关的一部分基础工作，也已经提前完成或基本完成。
- 整个 12 个月课题当然还没有完成；但中期前最核心的两件事，即“主案例跑通”和“形成可复查的实验材料”，已经基本具备。

如果按当前仓库主线来判断：
- `A1` 可以视为已完成当前阶段闭环。
- `A2` 也可以视为已完成当前阶段闭环。
- 当前最需要做的，不是继续重复同类型 rerun，而是把已有结果整理成导师易读、系统可提交的中期材料。

## 二、与 Timeline 的口径对齐说明
Timeline 原文中把首轮主案例写成：
- `A1 接缝裁剪（Seam Carving）`
- `A2 SVD 图像压缩`

实际推进过程中，A1 一直保持为 `Seam Carving` 主案例，没有发生变化。

A2 则发生了“范围收束”，不是偏离主线，而是从一个较宽的方向标签，收束到了一个更可复现、可评分、可教学化展示的具体任务。当前仓库中，A2 已统一固定为：
- `hw2-op2/src/chapter5_rslt.py::rslt_inpainting(...)`
- 任务性质：低秩图像任务族下、已完成范围收束并完成主案例闭环的灰度图像修复 / inpainting 主案例

这样调整的原因主要有三点：
1. 更容易固定协议，便于做可重复评测；
2. 更容易设计 bug-repair benchmark 和错误类型；
3. 更适合作为“过程化指导 + 结构化检查 + 纠错评估”的研究案例。

因此，中期阶段更准确的表述不是“A2 没按原计划做”，而是：

> A2 已完成从早期宽标签到当前研究主案例的范围收束，并在当前冻结口径下完成了主案例闭环。

## 三、过去几周的主要工作内容

下面按照 Timeline 的周次和阶段口径，对这几周的工作做一个尽量对齐的汇总。

### 1. 研究边界与仓库结构已经收敛
对应 Timeline 中的 `P0`、`W1`：
- 已确定首轮主案例聚焦 A1 / A2，而不是一开始就把题型铺得很大。
- 已建立仓库主目录结构，包括：
  - `runs/`
  - `metrics/`
  - `report/`
  - `task_cards/`
  - `prompts/`
  - `solutions/`
  - `outputs/`
- 已开始按 run 的方式保留研究日志，而不是只留零散截图和口头记录。

这一步的意义在于：
- 后续每次实验都能回溯；
- 中期/结题时可以直接复用现成材料；
- 研究方式从“零散试验”切换到“可复查研究流水线”。

### 2. A1 主案例已经从作业实现推进到研究闭环
对应 Timeline 中的 `W2-W5`：
- 已完成 A1 任务卡整理与主接口固定。
- 已补齐 A1 的参考实现、代码骨架、固定评测协议和对照项。
- 已建立 A1 的三种提示模式对照：
  - `direct_answer`
  - `plain_guidance`
  - `coe_guided`
- 已完成主实验、固定协议 replication 和 bug-repair benchmark。

目前 A1 已经不只是“做出一道题”，而是形成了下面这条完整链路：
1. 任务定义
2. Task Card
3. Prompt 模板
4. 代码产物
5. 固定协议评测
6. failure case 设计
7. bug-repair benchmark
8. 指标汇总与阶段总结

当前 A1 已形成的量化结果包括：
- `metrics/a1_guidance_eval_v0.csv`：`12` 条主记录
- `metrics/a1_codegen_perf_v0.csv`：`18` 条性能记录
- `metrics/a1_failure_repair_eval_v0.csv`：`9` 条 bug-repair 记录

A1 当前可支持的阶段性结论是：
- 三种模式在固定协议下都能稳定达到 runnable 和 correct；
- `plain_guidance` 与 `coe_guided` 在自检痕迹、回归说明和可检查性上明显更完整；
- 在当前 3 个最小失败样例上，结构化指导更能提升“诊断与复核质量”，而不是显著拉开修复通过率。

### 3. A2 主案例已从 bootstrap 发展为完整实验包
对应 Timeline 中的 `W2-W6` 以及 `P1` 的后半段：
- 早期已完成 A2 bootstrap 和问题边界整理；
- 随后把 A2 进一步收束为 `hw2-op2/src/chapter5_rslt.py::rslt_inpainting(...)` 主案例；
- 已整理 A2 的 requirement、Task Card、固定协议、bug-repair 协议、expanded-scope 协议；
- 已建立 A2 的三种提示模式对照；
- 已完成 baseline、fixed-protocol replication、bug-repair benchmark、expanded-scope validation；
- 截至目前，连 fresh-generation replication 也已经完成。

A2 当前已经形成的量化材料包括：
- `metrics/a2_guidance_eval_v0.csv`：`15` 条主记录
- `metrics/a2_recovery_perf_v0.csv`：`60` 条恢复结果记录
- `metrics/a2_failure_repair_eval_v0.csv`：`9` 条 bug-repair 记录
- `metrics/a2_expanded_scope_eval_v0.csv`：`3` 条 expanded-scope 主记录
- `metrics/a2_expanded_scope_perf_v0.csv`：`48` 条 expanded-scope case 记录

A2 当前已能支持的阶段性判断是：
- 三种模式在 A2 固定协议下都能一轮 runnable 且 correct；
- fixed-protocol 的 4 个 case 在 15 次运行中保持完全一致的 `(PSNR, SSIM, RSE)`；
- 说明当前三种模式的主要差异仍然在过程化自检、解释和可复查性，而不在最终恢复质量；
- expanded-scope validation 证明当前链路不只在最初的 4 个 fixed cases 上稳定；
- fresh-generation replication 说明当前结果不只是“复跑旧 artifact”，而是新生成产物在同协议下也能回到同一组固定结果。

### 4. 错误类型、反馈规则和 bug-repair 材料已经从零散样例变成可评分 benchmark
对应 Timeline 中的 `W7`：
- 已分别为 A1 / A2 设计 failure cases；
- 已形成最小复现材料，包括：
  - `symptom.md`
  - `buggy_code.py`
  - `diagnosis.md`
  - `fixed_code.py`
- 已建立 bug-repair 的固定评分口径；
- 已把这部分整理为可以直接复用的 benchmark，而不是临时挑 bug 演示。

当前 A1 的 3 个 failure cases 主要覆盖：
- DP 边界
- seam 删除后能量未重算
- 高度模式转置语义错误

当前 A2 的 3 个 failure cases 主要覆盖：
- mask 约束丢失
- mask 语义反转
- RPCA 分量写回错误

这一步的意义是：
- 研究不再只停留在“能不能生成代码”；
- 而是开始真正讨论“大模型在诊断、最小修补和回归验证上是否可教学化、可评估”。

### 5. 模板库、结构化检查清单和阶段总结文档已经开始成型
对应 Timeline 中的 `W5-W9`：
- 已形成 A1 / A2 的 prompt 模板资产；
- 已形成 Reviewer / Regressor 视角的结构化检查项；
- 已开始收束为中期可以直接引用的文档，包括：
  - Prompt 模板库
  - 结构化检查清单
  - 错误分类与反馈规则
  - 阶段小结
  - 附件清单

这部分说明项目已经不只是“做实验”，而是在沉淀可复用的方法资产。

## 四、当前已经形成的阶段性成果

### 1. 主案例成果
- `A1`：已形成完整闭环
- `A2`：已形成完整闭环

### 2. 方法成果
- 多角色协作流程已经在 A1 / A2 上有稳定落地材料；
- 结构化检查清单已经能覆盖输入、输出、不变量、评测协议和最小自检；
- bug-repair benchmark 已经可执行、可评分、可复查。

### 3. 文档成果
目前已经具备一批能直接给导师或中期系统看的材料，例如：
- 阶段报告
- 主案例协议
- 题目任务卡
- 正式 task-bank 索引页与状态页
- bug-repair 设计说明
- 模板库与检查清单
- 指标表和代表性 run 文档

### 4. 自动化链路成果
虽然当前还没有统一 GUI 或 Notebook 原型，但已经补出 `run_research_case.py` 这个最小 CLI 原型，并具备可复用的脚本化研究链路：
1. 读取题目要求 / Task Card / protocol / prompt
2. 生成代码产物
3. 执行固定协议评测或 bug-repair benchmark
4. 自动落 run 文档、输出目录和指标表

这意味着当前项目已经具备“原型雏形”，只是还没有封装成更完整的展示入口。

## 五、与 Timeline 阶段目标的对应关系

### 已基本完成的部分
对应 `P0 + P1`：
- 主案例范围已收敛；
- 仓库结构、run 日志、指标表已建立；
- A1 / A2 主案例已跑通；
- baseline 已有；
- failure cases 已形成；
- 过程化模板与结构化指导已经开始稳定复用。

### 已提前做掉一部分的内容
对应 `P2` 的前置内容：
- 模板资产已经不只是草稿，而是有真实 run 支撑；
- 错误分类与反馈规则已经开始形成文档化材料；
- 中期前可直接引用的阶段总结和附件清单已经可以组织出来。

### 尚未完成的部分
对应 Timeline 后续要求，目前仍然缺：
- 题库已经整理出 `16` 个中期正式条目，但仍不是结题版完整题库；
- `A3 / A4` 已各冻结 `1` 个首发条目并补齐 `requirement + taskcard_v1`，但尚未进入固定协议 / 正式实验；
- 更像教学产品的统一原型入口仍待继续封装；
- 小范围试用的实际招募、数据采集和量化对照图表；但试用方案、表单、评分表、记录模板和 CSV 数据结构已预埋。

所以当前更准确的阶段判断是：

> 主案例闭环已完成，但“扩展题库、教学原型、试用数据”仍处于中期收口后的后续推进阶段，不能把整个课题说成已经完成。

## 六、目前最稳妥的阶段性结论
当前最稳妥、最适合向导师和中期材料表述的结论是：

1. 本项目已经完成 A1 / A2 两个主案例的可复查实验闭环；
2. `plain_guidance` / `coe_guided` 的核心优势，是提升自检覆盖、根因说明和回归说明的可复查性；
3. 在 A2 这类冻结协议任务上，结构化 guidance 未必显著改变最终恢复指标，但确实改善了过程质量与可解释性；
4. 当前还不宜把结论写成“结构化 guidance 显著提升最终正确率”或“显著提高图像修复指标”；
5. 当前研究已经具备从 prompt、run、metrics 到 report 的完整证据链，具备中期汇报基础。

## 七、当前仍需导师确认或指导的点
结合 Timeline 中“导师同步点”的口径，目前最希望导师帮助确认的主要有三点：

### 1. 是否认可 A2 的范围收束
即是否同意把 A2 从早期较宽的 “SVD 图像压缩” 表述，正式收束成当前的 `hw2-op2/src/chapter5_rslt.py::rslt_inpainting(...)` 主案例。

### 2. 中期材料更强调哪一类成果
目前仓库既有：
- 主案例实验闭环
- 模板与检查清单
- failure-case benchmark

需要确认中期汇报时更应突出：
- 方法与流程和教育研究结论，
- 还是量化结果，
- 还是“教学化转化”的潜力。

### 3. 中期后优先级如何排
目前中期后可选的主方向包括：
- 把 A3 / A4 首发条目推进到固定协议和实验链路；
- 做统一 CLI / Notebook 原型；
- 开始小范围试用。

希望导师帮助判断哪个方向更值得优先投入。

## 八、下一步建议
结合 Timeline 和当前仓库状态，下一步建议按下面顺序推进：

1. 先把中期材料彻底收口，包括阶段小结、附件排序和导师版说明；
2. 优先收束模板库、检查清单、错误分类、题库页和原型入口；
3. 中期后再把 A3 / A4 首发条目推进到固定协议和实验链路，并继续把题库从“中期够用版”扩到更完整的任务家族；
4. 中期之后再进入小范围试用和学生反馈收集。

## 九、一句话总结
这几周的工作已经把项目从“课题想法 + 单次作业实验”，推进到了“有主案例、有固定协议、有 benchmark、有指标表、有阶段文档”的研究闭环状态。当前最缺的已经不是实验本身，而是把已有成果整理成中期可提交、导师可快速判断价值的材料包。
