# A4 Task Card v0

## 1. 题目一句话目标
建立 A4 扩展家族的题库占位入口，供中期后接入新的数学建模 / 图像融合类任务。

## 2. 当前定位
- 当前状态：`placeholder`
- 本卡只用于说明 A4 已进入题库骨架，不代表已经完成 requirement、固定协议或正式实验。
- 中期阶段不应把 A4 写成“已完成主案例”。

## 3. 候选子方向
- 微分方程模型
- Poisson 图像融合

## 4. 预期输入 / 输出
输入：
- 待冻结的具体题目定义
- 对应方程、数据、边界条件或图像输入约束

输出：
- A4 的 `requirement` 文档
- A4 的 `taskcard_v1`
- 固定协议、failure case 与 run / metrics / report 接入点

## 5. 接入前必须先冻结的内容
- 先从候选方向中确定 1 个首发条目。
- 明确输入 / 输出接口与最小正确性标准。
- 明确是偏“数值求解”还是“图像处理”。
- 明确固定协议与最小 failure-case 设计方式。

## 6. 中期阶段可诚实表述的状态
- A4 已完成题库占位接入。
- A4 尚未进入当前“已闭环主案例”范围。
- A4 的 requirement、协议、实验与结论仍待下一阶段补入。

## 7. 下一步最小动作
1. 在 A4 候选方向中冻结 1 个具体任务。
2. 补 `requirement` 文档。
3. 将本卡升级为 `taskcard_v1`。
